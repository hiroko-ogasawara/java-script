// 1. 変数にtrueを代⼊し、ifの条件がtrueの時にconsole.log();で変数の中⾝を表⽰させる
var boolean = true;
if (boolean) {
    console.log(boolean);
};

// 2. 変数に100を代⼊し、ifの条件がtrueの時にconsole.log();で変数の中⾝を表⽰させる
var num = 100
if (num === 100) {
    console.log(num);
}

// 3. 変数に"JavaScript"を代⼊し、ifの条件がtrueの時にconsole.log();で変数の中⾝を表⽰させる
var text = "JavaScript"
if (text === "JavaScript") {
    console.log(text);
}